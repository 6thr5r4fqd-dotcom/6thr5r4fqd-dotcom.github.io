---
layout: default
---
<article class="post">
  <header class="post-head">
    <h1 class="post-title">{{ page.title }}</h1>
    <div class="post-meta">{{ page.date | date: "%d-%m-%Y" }}</div>

    {% if page.image %}
      <img class="post-hero" src="{{ page.image }}" alt="">
    {% endif %}

    {% if page.excerpt %}
      <p class="post-excerpt">{{ page.excerpt }}</p>
    {% endif %}
  </header>

  <div class="post-body">
    {{ content }}
  </div>
</article>
